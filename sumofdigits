#include<stdio.h>
int main()
{
	int a;
	int sum,t,rem;
	printf("enter the number: ");
	scanf("%d",&a);
	t=a;
	while(t != 0)
	{
		rem = t % 10;
		sum = sum + rem;
		t = t / 10;
	}
	printf("the sum of the digits of the given number is %d",sum);
	return 0;
}

